# Summary: 03.1-02 — Wire Toolbar F-Key Buttons

**Status:** Complete
**Requirement:** BUG-02

## What was done
- Added `global-action` CustomEvent listener in `FileBrowserPane.tsx`
- Listener only activates when `isActive === true` (matches keydown guard)
- Maps event detail values: F3->readOnly editor, F4->edit mode, F5->copy dialog, F6->move dialog, F8->delete confirm, Arc->archive dialog
- Existing keydown listener for physical F-keys unchanged (no regression)

## Files changed
- `src/renderer/panes/file-browser/FileBrowserPane.tsx`

## Verification
- Toolbar F3/F4/F5/F6/F8/Archive buttons now trigger correct actions when a file-browser pane is active
- Physical keyboard F3-F8 keys still work (separate listener, unchanged)
- Terminal/Notes pane active -> toolbar buttons silent no-op (no crash)

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED
- `src/renderer/panes/file-browser/FileBrowserPane.tsx` modified with new useEffect at lines 162-179
- Commit c1a9d81 verified in git log
